# دليل دراسة الكود — نظام التعرّف على تعابير الوجه

> دليل موجّه لمن ينضم إلى المشروع حديثاً. الهدف أن تفهم بنية الكود ومنطقه خلال ساعة أو ساعتين، وتعرف أين تُعدّل عند الحاجة.

---

## 1. ما الذي يفعله المشروع؟

نظام يصنّف تعبير الوجه في صورة إلى **٧ فئات**: `surprise, fear, disgust, happy, sad, angry, neutral`.

يتألف من جزأين:

- **جزء التعلّم العميق**: قراءة البيانات، تدريب نموذج ResNet-50، تقييمه.
- **تطبيق ويب**: يرفع المستخدم صورة فيحصل على التصنيف مع خريطة انتباه تفسيرية.

**الأداء الحالي:** دقّة ٨٣.٨٪ و macro-F1 يساوي 0.759 على مجموعة اختبار RAF-DB.

> **ملاحظة مهمة عن النطاق:** النظام يتعرّف على *تعبير الوجه الظاهر*، وليس على المشاعر الداخلية الحقيقية، وهو **ليس أداة تشخيص طبي**.

---

## 2. التشغيل السريع (ابدأ من هنا)

```bash
git clone <رابط-المستودع>
cd emotion-classification
python -m venv .venv && .venv\Scripts\Activate.ps1     # ويندوز
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
pip install -r requirements.txt
```

ثم ضع ملف النموذج المدرّب في `outputs/best_model.pth` (غير موجود في git لأنه كبير)، وشغّل:

```bash
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

وافتح <http://127.0.0.1:8000>.

**للتحقق من سلامة البيئة:**

```bash
pytest -q          # يجب أن تنجح ٢٠ اختباراً، ولا تحتاج بيانات
```

---

## 3. الصورة الكبرى: مسار البيانات

فهم هذين المسارين يكفي لفهم ٨٠٪ من المشروع.

### مسار التدريب

```
مجلدات الصور (data/rafdb/DATASET/train/1..7)
        │
        ▼  dataset.py  →  قراءة الملفات + خريطة التسميات + تقسيم train/val/test
        │
        ▼  preprocessing.py  →  تغيير القياس + زيادة البيانات + التطبيع
        │
        ▼  classifier.py  →  ResNet-50 + رأس التصنيف
        │
        ▼  train.py  →  حلقة التدريب (خسارة، أمثلة، تحقّق، إيقاف مبكر)
        │
        ▼  outputs/best_model.pth
```

### مسار الاستدلال (التطبيق)

```
صورة يرفعها المستخدم
        │
        ▼  face_preprocess.py  →  كشف الوجه + محاذاة العينين + قصّ 224×224
        │
        ▼  preprocessing.py  →  تطبيع (بدون زيادة بيانات)
        │
        ▼  النموذج + TTA  →  احتمالات ٧ فئات
        │
        ▼  gradcam.py  →  خريطة الانتباه
        │
        ▼  main.py  →  JSON إلى الواجهة
```

---

## 4. خريطة الملفات (مرتّبة بترتيب القراءة المقترح)

اقرأ الملفات بهذا الترتيب، فكل ملف يبني على سابقه.

### المستوى الأول: الأساسيات

| # | الملف | الأسطر | ما يفعله |
|---|---|---|---|
| 1 | `configs/default.yaml` | — | **ابدأ هنا.** كل المعاملات: البيانات، النموذج، التدريب، زيادة البيانات. |
| 2 | `src/config.py` | 190 | يقرأ ملف YAML أعلاه ويتحقّق من صحّته، ويحوّله إلى كائنات `dataclass`. |

**ما يجب أن تفهمه:** لا توجد مسارات أو أرقام مكتوبة داخل الكود (hardcoded). كل شيء يأتي من ملف الإعدادات. لتغيير أي سلوك، عدّل الـ YAML أولاً.

### المستوى الثاني: البيانات

| # | الملف | الأسطر | ما يفعله |
|---|---|---|---|
| 3 | `src/data/preprocessing.py` | 83 | أبسط ملف. `build_transforms()` تُرجع سلسلة التحويلات: تدريب (مع زيادة بيانات) أو تقييم (حتمية). |
| 4 | `src/data/dataset.py` | 461 | **الأهم في قسم البيانات.** قراءة الصور، خريطة التسميات، التقسيم، أوزان الفئات. |
| 5 | `src/data/face_preprocess.py` | 202 | كشف الوجه ومحاذاته. يُستخدم **فقط** وقت الاستدلال على صور جديدة. |

**الدوال المفتاحية في `dataset.py`:**

- `build_label_map()` — يحوّل ترميز RAF-DB (يبدأ من ١) إلى فهارس النموذج (تبدأ من ٠).
- `RAFDBDataset` — صنف الـ Dataset القياسي في PyTorch، وأهم دواله `__getitem__` و `compute_class_weights()` و `sample_weights()`.
- `build_datasets()` — الدالة الرئيسية: تُرجع `(train, val, test)` جاهزة.

### المستوى الثالث: النموذج والتدريب

| # | الملف | الأسطر | ما يفعله |
|---|---|---|---|
| 6 | `src/models/classifier.py` | 111 | `EmotionClassifier`: يحمّل ResNet-50، يجمّد جزءاً منه، يضيف رأس تصنيف. |
| 7 | `src/engine/utils.py` | 80 | أدوات مساعدة: `set_seed`، حفظ/تحميل النموذج، `AverageMeter`. |
| 8 | `src/engine/train.py` | 266 | حلقة التدريب الكاملة. |
| 9 | `src/engine/evaluate.py` | 153 | المقاييس ومصفوفة الالتباس و TTA. |

**بنية النموذج باختصار:**

```
صورة (3×224×224)
   ↓ ResNet-50 backbone (مُدرَّب مسبقاً على ImageNet، نصفه مجمّد)
خرائط خصائص (2048 قناة)
   ↓ Global Average Pooling
   ↓ Linear(2048 → 256) → ReLU → Dropout(0.5)
   ↓ Linear(256 → 7)
Logits (٧ قيم خام، بدون Softmax)
```

### المستوى الرابع: التفسير والتطبيق

| # | الملف | الأسطر | ما يفعله |
|---|---|---|---|
| 10 | `src/interpret/gradcam.py` | 98 | خريطة الانتباه الحرارية. `compute_gradcam_overlay()` للتطبيق، `generate_gradcam()` لحفظ ملفات. |
| 11 | `src/inference.py` | 134 | التنبؤ على صورة أو مجلد، وإخراج CSV. |
| 12 | `app/main.py` | 205 | خادم FastAPI: `/api/health` و `/api/predict`. |
| 13 | `app/static/*` | ~900 | الواجهة: HTML و CSS و JavaScript. |

### نقاط الدخول (scripts)

ملفات رفيعة لا تحتوي منطقاً، فقط `argparse` تستدعي `src/`:

```bash
python scripts/download_data.py    # التحقّق من وجود البيانات
python scripts/train.py            # التدريب
python scripts/evaluate.py --checkpoint outputs/best_model.pth --tta --gradcam
python scripts/predict.py --checkpoint outputs/best_model.pth --input <صورة أو مجلد>
```

---

## 5. مفاهيم أساسية في هذا المشروع

### أ. خريطة التسميات (حسّاسة جداً)

RAF-DB ترقّم الفئات من ١ إلى ٧، والنموذج يستخدم فهارس من ٠ إلى ٦. الربط **صريح** في الكود ولا يُفترض أبجدياً:

| RAF-DB | 1 | 2 | 3 | 4 | 5 | 6 | 7 |
|---|---|---|---|---|---|---|---|
| التعبير | surprise | fear | disgust | happy | sad | angry | neutral |
| فهرس النموذج | 0 | 1 | 2 | 3 | 4 | 5 | 6 |

خطأ في هذا الربط يُنتج نموذجاً يتدرّب على تسميات خاطئة تماماً بينما تبدو الأرقام طبيعية.

### ب. عدم توازن الفئات

الفارق ضخم: السعادة ٤,٧٧٢ صورة مقابل الخوف ٢٨١ صورة (١٧ ضعفاً). يُعالَج بطريقتين:

- `use_class_weights` — وزن أعلى لخسارة الفئات النادرة.
- `balanced_sampler` — إعادة أخذ العيّنات ليتساوى ظهور الفئات.

> ⚠️ **لا تفعّلهما معاً** — ذلك تصحيح مزدوج يضرّ بالأداء. الكود يطبع تحذيراً إن فعلت.

### ج. المحاذاة (سبب خطأ حقيقي وقع سابقاً)

صور التدريب مقصوصة ومحاذاة مسبقاً. لو أدخلت صورة كاملة (وجه + خلفية + شعر) إلى النموذج فستنخفض الدقّة بشدّة. لذلك `face_preprocess.py` يكشف العينين ويطبّق تحويلاً تشابهياً ينقلهما إلى موضع ثابت مُستخرَج قياسياً من صور RAF-DB.

**مثال واقعي:** صورة لامرأة متفاجئة أعطت `fear 48%` بالصورة الكاملة، وأعطت `surprise 90%` بعد القصّ الصحيح.

### د. TTA (زيادة البيانات وقت الاختبار)

يُمرَّر الصورة وانعكاسها الأفقي، ويُؤخذ متوسط الاحتمالات. مكسب مجاني تقريباً (‎+0.6٪). مفعّل افتراضياً في الـ API.

### هـ. اختيار أفضل نموذج بـ macro-F1 لا بالدقّة

مع اختلال الفئات، الدقّة مضلّلة (نموذج يتوقّع "سعيد" دائماً قد يحقّق دقّة مرتفعة). لذا يُحفَظ أفضل checkpoint بناءً على **macro-F1** الذي يعامل كل الفئات بالتساوي.

---

## 6. أين تُعدّل عند الحاجة؟

| تريد أن... | عدّل |
|---|---|
| تغيّر معاملات التدريب (lr، حجم الدفعة، الحقب) | `configs/default.yaml` فقط |
| تجرّب backbone آخر | `configs/default.yaml` → `model.backbone` (`resnet50` أو `efficientnet_b0`) |
| تضيف تحويل زيادة بيانات جديد | `src/data/preprocessing.py` → `build_transforms()` + حقل في `AugmentCfg` |
| تدعم صيغة بيانات جديدة | `src/data/dataset.py` → دالة `_scan_*` جديدة + فرع في `build_datasets()` |
| تغيّر رأس التصنيف | `src/models/classifier.py` → `EmotionClassifier.__init__` |
| تضيف مقياساً جديداً | `src/engine/evaluate.py` → `compute_metrics()` |
| تضيف endpoint في الـ API | `app/main.py` |
| تعدّل الواجهة | `app/static/index.html` و `styles.css` و `app.js` |

**قاعدة عامة:** إن وجدت نفسك تكتب رقماً أو مساراً داخل ملف `.py`، فالأرجح أن مكانه الصحيح هو ملف الإعدادات.

---

## 7. مزالق واقعية في هذا المشروع

هذه مشاكل حدثت فعلاً أثناء التطوير، اقرأها لتوفّر على نفسك الوقت:

1. **تسرّب البيانات (Data Leakage):** يوجد `TODO` في `dataset.py` ينبّه إلى ضرورة التقسيم على **مستوى الشخص** عند استخدام صور تطبيق حقيقية. ظهور صور الشخص نفسه في التدريب والاختبار يرفع الأرقام زيفاً.

2. **انكسار واجهة MediaPipe:** النسخة 0.10 حذفت `mp.solutions` القديمة. الكود الآن يستخدم واجهة Tasks الجديدة مع كاشف OpenCV احتياطياً. الخطر أن الفشل كان **صامتاً**: كل تنبؤ كان يعمل على الصورة الكاملة دون أي رسالة خطأ.

3. **ذاكرة المتصفح المؤقتة:** بعد تعديل ملفات الواجهة قد ترى نسخة قديمة. أُضيف middleware يمنع التخزين المؤقت، وإن استمرت المشكلة اضغط `Ctrl+Shift+R`.

4. **الملفات غير الموجودة في git:** مجلدا `data/` و `outputs/` مستثنيان. الاستنساخ يعطي الكود فقط، وعليك نقل `best_model.pth` يدوياً.

5. **`num_workers` على ويندوز:** قد يسبّب بطئاً أو أخطاء أحياناً؛ اضبطه على `0` عند ظهور مشاكل في الـ DataLoader.

6. **التدريب الكامل للشبكة أضرّ فعلياً:** جُرّب إلغاء التجميد الكامل (`freeze_ratio: 0.0`) فانخفض الأداء إلى ٧٢٪ بسبب نقص التخصيص. القيمة الحالية `0.5` هي الأفضل تجريبياً.

---

## 8. الاختبارات

```bash
pytest -q                          # كل الاختبارات (٢٠)
pytest tests/test_dataset.py -v    # ملف محدّد
```

| الملف | يغطّي |
|---|---|
| `tests/test_dataset.py` | خريطة التسميات، شكل المخرجات، أوزان الفئات، التقسيم، دمج بيانات إضافية |
| `tests/test_model.py` | شكل مخرجات النموذج، التجميد، الـ backbones المدعومة |
| `tests/test_face_preprocess.py` | تحميل الكاشف، والسلوك عند غياب الوجه |

الاختبارات تستخدم **بيانات اصطناعية**، فتعمل دون تنزيل أي مجموعة بيانات. أي تعديل في `src/` يجب أن تتبعه `pytest`.

---

## 9. تمارين للتحقّق من الفهم

جرّب هذه بالترتيب؛ إن نجحت فيها فأنت تفهم المشروع:

1. **سهل:** غيّر `train.batch_size` في الـ YAML وشغّل التدريب حقبة واحدة. أين قُرئت القيمة؟
2. **سهل:** شغّل التقييم مع `--tta` وبدونه. كم الفرق في الدقّة؟
3. **متوسّط:** أضف تحويل زيادة بيانات جديد (مثل `RandomGrayscale`) واجعله قابلاً للضبط من ملف الإعدادات.
4. **متوسّط:** اطبع توزيع الفئات في `train_ds` و `val_ds`. هل التقسيم الطبقي يعمل فعلاً؟
5. **متقدّم:** أضف مقياس AUROC إلى `compute_metrics()` واعرضه في التقرير.
6. **متقدّم:** تتبّع مسار صورة من رفعها في الواجهة حتى ظهور النتيجة، وسمِّ كل دالة تمرّ بها.

---

## 10. مراجع سريعة

- **التوثيق العام:** `README.md`
- **التوثيق النظري:** ملف Word خارج المستودع (الفصول: مقدمة، نظري، عملي)
- **الأوراق العلمية الأساسية:** ResNet (He et al. 2016) · RAF-DB (Li et al. 2017) · AffectNet (Mollahosseini et al. 2019) · Grad-CAM (Selvaraju et al. 2017)

---

**أسئلة؟** ابدأ بقراءة `configs/default.yaml` ثم `src/data/preprocessing.py` (أبسط ملف)، ثم تدرّج حسب الترتيب في القسم ٤.
