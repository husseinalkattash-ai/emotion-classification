# Install PyTorch with CUDA support FIRST, matching your CUDA version, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# (see https://pytorch.org/get-started/locally/ for the right command).
# Then: pip install -r requirements.txt
torch
torchvision
numpy
pandas
pillow
opencv-python-headless
mediapipe
scikit-learn
matplotlib
seaborn
pyyaml
tqdm
tensorboard
grad-cam
pytest
fastapi
uvicorn[standard]
python-multipart
